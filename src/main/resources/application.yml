spring:
  application:
    name: GestorRH-API
  profiles:
    active: dev

  # LOGS
  security:
    user:
      name: usuario_ignorado
      password: sin_uso
  jpa:
    open-in-view: false

  # CONFIGURACIÓN DE SUBIDA DE ARCHIVOS
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

server:
  port: 8080

# RUTA RELATIVA PARA JUSTIFICANTES
file:
  upload-dir: uploads/ausencias

# CONFIGURACIÓN DE SEGURIDAD (JWT).
jwt:
  secret: ${JWT_SECRET:mi_clave_secreta_de_desarrollo_local_gestorrh_2026_super_segura}
  expiration: 86400000

# CONFIGURACIÓN DE SWAGGER / OPENAPI
springdoc:
  swagger-ui:
    tags-sorter: alpha
    operations-sorter: method