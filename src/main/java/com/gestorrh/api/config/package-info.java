/**
 * Clases de configuración global de la API.
 * <p>
 * Contiene la parametrización de seguridad (ConfigSeguridad), la configuración de
 * Swagger/OpenAPI para la documentación (ConfigSwagger) y, solo para pruebas iniciales, el componente encargado
 * de la carga inicial de datos en la base de datos (DataSeeder).
 * </p>
 */
package com.gestorrh.api.config;